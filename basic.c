/*
# include<stdio.h>

int main() {
    int age;
    printf("enter age:");
    scanf("%d", &age);
    printf("age is : %d",age);
    return 0;
*/

/* add
# include<stdio.h>

float main() {
    int a,b;
    printf("enter value of a:");
    scanf("%d", &a);

    printf("enter value of b:");
    scanf("%d", &b);

    printf("sum is : %d", a+b);
    return 0;
}
*/ 

# include<stdio.h>

int main() {
    int side;
    printf("enter side :");
    scanf("%d", &side);
    printf("Area of sq. is : %d", side*side);
    return 0;
}